import { useLoginMutation } from "./login";
import { useLogoutMutation } from "./logout";
import { useRegisterMutation } from "./register";

export const MUTATIONS = {
  useLoginMutation,
  useLogoutMutation,
  useRegisterMutation,
};
